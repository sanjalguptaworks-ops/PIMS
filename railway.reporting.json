{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @pims/db exec prisma generate && pnpm --filter @pims/reporting run build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @pims/db exec prisma migrate deploy && pnpm --filter @pims/reporting exec next start -p $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
